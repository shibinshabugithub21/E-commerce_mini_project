<%-include("header") %>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
<style type="text/css">
    body {
        margin-top: 20px;
        background-color: #f2f6fc;
        color: #69707a;
    }

    .img-account-profile {
        height: 10rem;
    }

    .rounded-circle {
        border-radius: 50% !important;
    }

    .card {
        box-shadow: 0 0.15rem 1.75rem 0 rgb(33 40 50 / 15%);
    }

    .card .card-header {
        font-weight: 500;
    }

    .card-header:first-child {
        border-radius: 0.35rem 0.35rem 0 0;
    }

    .card-header {
        padding: 1rem 1.35rem;
        margin-bottom: 0;
        background-color: rgba(33, 40, 50, 0.03);
        border-bottom: 1px solid rgba(33, 40, 50, 0.125);
    }

    .form-control,
    .dataTable-input {
        display: block;
        width: 100%;
        padding: 0.875rem 1.125rem;
        font-size: 0.875rem;
        font-weight: 400;
        line-height: 1;
        color: #69707a;
        background-color: #fff;
        background-clip: padding-box;
        border: 1px solid #c5ccd6;
        -webkit-appearance: none;
        -moz-appearance: none;
        appearance: none;
        border-radius: 0.35rem;
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    }

    

    .nav-borders .nav-link {
        color: #69707a;
        border-bottom-width: 0.125rem;
        border-bottom-style: solid;
        border-bottom-color: transparent;
        padding-top: 0.5rem;
        padding-bottom: 0.5rem;
        padding-left: 0;
        padding-right: 0;
        margin-left: 1rem;
        margin-right: 1rem;
    }
</style>

<div class="container-xl px-4 mt-4">
    <nav class="nav nav-borders">
        <a class="nav-link active ms-0" href="/profile">Profile</a>
        <a class="nav-link" href="/managePassword" >Manage Password</a>
        <a class="nav-link" href="/manageAddress" >Manage Addres</a>
        <a class="nav-link" href="/order">Order</a>
    </nav>
    <hr class="mt-0 mb-4">
    <div class="row">
        <div class="col-xl-4">
            <!-- Profile picture card-->
            <div class="card mb-4 mb-xl-0">
                <div class="card-header">Profile Picture</div>
                <div class="card-body text-center">
                    <!-- Profile picture image-->
                    <img class="img-account-profile rounded-circle mb-2" src="http://bootdey.com/img/Content/avatar/avatar1.png" alt="">
                    <!-- Profile picture help block-->
                    <div><%= userData.name %></p></div>
                    <!-- Profile picture upload button-->
                    <button class="btn btn-primary" type="button">Upload new image</button>
                </div>
            </div>
            </div>
        <div class="col-xl-8">
            <div class="card mb-4">
                <div class="card-header">Account Details</div>
                <div class="card-body">
                    <form action="/updateprofile" method="POST" >
                        <div class="mb-3">
                            <label class="small mb-1" for="firstname">Fullname</label>
                            <input class="form-control" name="name" id="firstname" type="text" value="<%= userData.name %>"
                                required>
                            <div id="firstnameValidation" class="invalid-feedback"></div>
                        </div>
                        <div class="row gx-3 mb-3">
                            <div class="col-md-6">
                                <label class="small mb-1" for="Address">House Name</label>
                                <input class="form-control" name="houseName" id="Address" type="text"
                                    value="<%= userData.profile.address[0]?.houseName%>"
                                    required>
                                <div id="addressValidation" class="invalid-feedback"></div>
                            </div>
                            <div class="col-md-6">
                                <label class="small mb-1" for="Pincode">Pincode</label>
                                <input class="form-control" name="postalCode" id="pincode" type="text"
                                    value="<%= userData.profile.address[0]?.postalCode%>"
                                    required>
                                <div id="pincodeValidation" class="invalid-feedback"></div>
                            </div>
                            <div class="col-md-6">
                                <label class="small mb-1" for="inputLocation">City</label>
                                <input class="form-control" name="city" id="inputLocation" type="text"
                                    value="<%= userData.profile.address[0]?.city%>"
                                    required>
                                <div id="cityValidation" class="invalid-feedback"></div>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="small mb-1" for="inputEmailAddress">Email </label>
                            <input class="form-control" name="emailid" id="inputEmailAddress" type="email"
                                value="<%= userData.email %>" required>
                            <div id="emailValidation" class="invalid-feedback"></div>
                        </div>
                        <div class="row gx-3 mb-3">
                            <div class="col-md-6">
                                <label class="small mb-1" for="inputPhone">Phone number</label>
                                <input class="form-control" name="phone" id="inputPhone" type="tel"
                                    value="<%= userData.phone %>" required>
                                <div id="phoneValidation" class="invalid-feedback"></div>
                            </div>
                            <button class="btn btn-primary mt-3" type="submit">Save changes</button>
                        </div>
                    </form>
                </div>
            </div>
            
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script>
<!-- <script type="text/javascript">
    function validateForm() {
        var valid = true;

        // Validate firstname
        var firstname = document.getElementById("firstname").value;
        if (firstname.trim() === "") {
            document.getElementById("firstnameValidation").innerHTML = "Firstname is required";
            valid = false;
        } else {
            document.getElementById("firstnameValidation").innerHTML = "";
        }

        // Validate address
        var address = document.getElementById("Address").value;
        if (address.trim() === "") {
            document.getElementById("addressValidation").innerHTML = "Address is required";
            valid = false;
        } else {
            document.getElementById("addressValidation").innerHTML = "";
        }

        // Validate pincode
        var pincode = document.getElementById("pincode").value;
        var pincodeRegex = /^\d{6}$/; // 6 digit pincode
        if (!pincodeRegex.test(pincode)) {
            document.getElementById("pincodeValidation").innerHTML = "Enter a valid pincode (6 digits)";
            valid = false;
        } else {
            document.getElementById("pincodeValidation").innerHTML = "";
        }

        // Validate city
        var city = document.getElementById("inputLocation").value;
        if (city.trim() === "") {
            document.getElementById("cityValidation").innerHTML = "City is required";
            valid = false;
        } else {
            document.getElementById("cityValidation").innerHTML = "";
        }

        // Validate email
        var email = document.getElementById("inputEmailAddress").value;
        var emailRegex = /^\S+@\S+\.\S+$/; // Basic email regex
        if (!emailRegex.test(email)) {
            document.getElementById("emailValidation").innerHTML = "Enter a valid email address";
            valid = false;
        } else {
            document.getElementById("emailValidation").innerHTML = "";
        }

        // Validate phone
        var phone = document.getElementById("inputPhone").value;
        var phoneRegex = /^\d{10}$/; // 10 digit phone number
        if (!phoneRegex.test(phone)) {
            document.getElementById("phoneValidation").innerHTML = "Enter a valid 10-digit phone number";
            valid = false;
        } else {
            document.getElementById("phoneValidation").innerHTML = "";
        }

        return valid;
    }
</script> -->

<%-include("footer") %>
