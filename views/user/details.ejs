<%-include("header")-%>
<style>
    * {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    :root {
        --primary-color: #5344db;
        --accent-color: #5344db;
        --grey: #484848;
        --bg-grey: #efefef;
        --shadow: #949494;
    }

    .single-product {
        display: flex;
        justify-content: space-between;
        margin-top: 20px; /* Adjust margin as needed */
    }

    .product-image {
        flex: 1;
        margin-right: 20px; /* Adjust margin as needed */
        position: relative;
    }

    .product-image-main {
    height: 300px;
    /* background: var(--bg-grey); */
    overflow: hidden;
    margin-left: 63px;
    }
    .product-image-main img {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    .product-image-slider {
        display: flex;
        gap: 10px;
        margin: 50px 20px;
        justify-content: center;
    }

    .product-image-slider img {
        width: 15%;
        height: 50%;
        /* background: var(--bg-grey); */
        padding: 6px;
        cursor: pointer;
    }

    .product-details {
        flex: 1;
        /* margin-left: 20px; */
    }

    .product-title h2 {
        font-size: 32px;
        line-height: 2.4rem;
        font-weight: 700;
        letter-spacing: -0.02rem;
    }

    .product-rating {
        display: flex;
        margin-top: 4px;
        margin-bottom: 10px;
        align-items: center;
    }

    .product-rating span:not(:last-child) {
        color: #ffc600;
    }

    .product-rating .review {
        color: var(--grey);
        font-size: 12px;
        font-weight: 500;
    }

    .product-price {
        display: flex;
        position: relative;
        margin: 10px 0;
        align-items: center;
    }

    .product-price .offer-price {
        font-size: 48px;
        font-weight: 700;
    }

    .product-price .sale-price {
        font-size: 22px;
        font-weight: 500;
        text-decoration: line-through;
        color: var(--grey);
        margin-left: 12px;
    }

    .product-details h3 {
        font-size: 18px;
        font-weight: 500;
    }

    .product-details p {
        margin: 5px 0;
        font-size: 14px;
        line-height: 1.2rem;
    }

    /* Zoom styles */
   /* Zoom styles */
.container-img {
    position:absolute;
    /* overflow: hidden; */
}

.image {
    width: 100%;
    height: auto;
}

.lens {
    width: 100px;
    height: 100px;
    /* position: relative; */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    /* background-color: rgba(255, 255, 255, 0.6); */
    /* display: none; */
}

.result {
    position: absolute;
    left: 100%; /* Change this to the desired position */
    top: 0;
    width: 500px;
    height: 500px;
    background-repeat: no-repeat;
    background-color: white;
    /* display: none; */
}


    .product-image:hover .lens,
    .product-image:hover .result {
        display: block;
    }

    .breadcrumb {
        background: #48484810;
        padding: 8px 4px;
        border-radius: 8px;
        font-size: 15px;
    }

    .breadcrumb span {
        display: inline-flex;
        flex-direction: row;
        gap: 8px;
        margin-left: 8px;
    }

    .breadcrumb span:not(:last-child)::after {
        content: '/';
    }

    .breadcrumb span a {
        text-decoration: none;
        color: var(--primary-color);
    }

    .divider {
        display: block;
        height: 1px;
        width: 100%;
        background: #48484830;
        margin: 20px 0;
    }

    .product-btn-group {
        display: flex;
        gap: 15px;
    }

    .product-btn-group .button {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 4px;
        padding: 10px 24px;
        font-size: 16px;
        font-weight: 500;
    }

    .product-btn-group .buy-now {
        background-color:blue;
        color: #fff;
        border: 1px solid var(--accent-color);
        border-radius: 4px;
        cursor: pointer;
    }

    .product-btn-group .buy-now i {
        font-size: 20px;
    }

    .product-btn-group .buy-now:hover {
        box-shadow: 0 3px 6px var(--shadow);
    }

    .product-btn-group .add-cart {
        background-color:orange;
        color:black;
        border-radius: 4px;
        cursor: pointer;
    }

    .product-btn-group .add-cart i {
        font-size: 20px;
    }

    .product-btn-group .add-cart:hover {
        box-shadow: 0 3px 6px var(--shadow);
        background: var(--grey);
        color: #fff;
    }

    .product-btn-group .heart {
        color: var(--grey);
        cursor: pointer;
    }

    .product-btn-group .heart i {
        font-size: 20px;
    }

    .product-btn-group .heart:hover {
        color: var(--accent-color);
    }

    /* Responsive Mobile */
    @media screen and (max-width: 520px) {
        .container {
            padding: 20px;
            height: auto;
        }

        .row {
            display: block;
        }

        .col-6 {
            width: 100%;
            display: block;
        }

        .product-image-main {
            width: 100%;
            height: 200px;
        }

        .product-image-slider {
            gap: 5px;
            margin: 50px;
        }

        .breadcrumb {
            display: none;
        }

        .product-title h2 {
            font-size: 24px;
            line-height: 1.6rem;
        }

        .product-size {
            display: block;
        }

        .product-size .size-layout {
            display: block;
            margin: 10px 0;
        }

        .product-size .size-layout .size {
            padding: 6px 10px;
        }

        .product-btn-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
    }
    .product-price {
    display: flex;
    position: relative;
    margin: 10px 0;
    align-items: center;
}

.product-price .sale-price {
    font-size: 22px;
    font-weight: 500;
    text-decoration: line-through; /* Add strike-through */
    color: var(--grey);
    margin-right: 12px; /* Adjust margin as needed */
}

.product-price .offer-price {
    font-size: 48px;
    font-weight: 700;
color: black;
}


    /* Responsive Tablet */
    @media (min-width: 520px) and (max-width: 1080px) {
        .container {
            display: block;
            height: auto;
            padding: 20px;
        }
    }

</style>

<div class="container">
    <% product.forEach(element => { %>
    <div class="single-product">
        <div class="product-image">
            <div class="product-image-main">
                <div class="container-img">
                    <div id="zoomContainer-<%= element.id %>" class="zoom-container">
                    <img id="image-<%= element.id %>" class="card-img-top image" style="width: 100%; height: 21.1em;" src="/productImages/<%= element.Image[0] %>" />
                    <div class="lens" data-productid="<%= element.id %>"></div>
                    <div class="result" data-productid="<%= element.id %>" style="z-index: 1000;"></div>
                </div>
                </div>
            </div>
            <div class="product-image-slider">
                <!-- Update IDs for zoom functionality -->
                <% element.Image.forEach((img, index) => { %>
                    <img id="zoom-image-<%= element._id %>-<%= index %>" class="card-img-top" style="width: 15%; height: 50%;" src="/productImages/<%= img %>" onclick="changeMainImage('<%= element.id %>', '<%= img %>')" />
                <% }) %>
            </div>
        </div>
        
        <div class="product-details">
            <div class="breadcrumb">
                <span><a href="#">Home</a></span>
                <span><a href="#">Product</a></span>
                <span class="active">details</span>
            </div>

            <div class="product">
                <div class="product-title">
                    <h2><%= element.Productname %></h2>
                </div>
                <div class="product-rating">
                    <span><i class="bx bxs-star"></i></span>
                    <span><i class="bx bxs-star"></i></span>
                    <span><i class="bx bxs-star"></i></span>
                    <span><i class="bx bxs-star"></i></span>
                    <span><i class="bx bxs-star"></i></span>
                    <span class="review">(<%= element.Rating %>.rating)</span>
                </div>
                
                <div class="d-flex flex-column">
                    <div class="product-price">
                        <!-- Original price with strike-through -->
                        <span class="sale-price">₹<%= element.Price%></span>
                        
                        <!-- Offer price -->
                        <span class="offer-price">₹<%= element.OriginalPrice %></span>
                    </div>
    
                    <div class="product-details mb-4">
                        <h3>About</h3>
                        <p><%= element.Description %></p>
                    </div>
    
                    <div class="product-btn-group">
                        <form action="/cart/<%= element._id %>" method="post">
                            <button class="add-to-cart btn btn-warning"  data-product-id="<%= element._id %>" data-product-price="<%= element.Price %>" type="submit">Add to Cart</button>
                        </form>                  
                        <!-- <button class="buy-now btn btn-primary" onclick="location.href='/CheckOutPage/<%= element._id %>'">Buy Now</button> -->

                    </div>
                </div>
            </div>

        </div>
    </div>
    <%})%>
</div>


<h1><center>Recommented Prdoducts</center></h1>


<div class="container mt-4">
    <div class="row row-cols-3">
        <% recommendedProducts.forEach(recommendedProduct => { %>
            <div class="col-md-4 col-lg-3">
                <div class="card">
                    <img class="card-img-top" style="width: 100%; height: 21.1em;" src="/productImages/<%= recommendedProduct.Image[0] %>" />
                    <div class="card-body">
                        <h5 class="card-title"><%= recommendedProduct.Productname %></h5>
                        <span class="offer-price">₹<%= recommendedProduct.OriginalPrice %></span>
                        <div class="d-flex justify-content-between align-items-center">
                            <form action="/cart/<%= recommendedProduct._id %>" method="post">
                                <button class="add-to-cart btn btn-warning"  data-product-id="<%= recommendedProduct._id %>" data-product-price="<%= recommendedProduct.Price %>" type="submit">Add to Cart</button>
                            </form>
                            <a href="/details/<%= recommendedProduct._id %>" class="btn btn-primary">Buy Now</a>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>



<script>
   const containers = document.querySelectorAll(".container-img");

containers.forEach(container => {
    const lens = container.querySelector(".lens");
    const result = container.querySelector(".result");

    container.addEventListener("mousemove", zoomImage);
    container.addEventListener("mouseenter", () => {
        lens.style.display = "block";
        result.style.display = "block";
    });
    container.addEventListener("mouseleave", () => {
        lens.style.display = "none";
        result.style.display = "none";
    });

    function zoomImage(e) {
        const productId = container.querySelector(".lens").getAttribute("data-productid");
        const image = container.querySelector(`#image-${productId}`);
        const lens = container.querySelector(".lens");
        const result = container.querySelector(".result");

        const containerRect = container.getBoundingClientRect();
        const lensRect = lens.getBoundingClientRect();
        const resultRect = result.getBoundingClientRect();

        const { x, y } = getMousePos(e);

        lens.style.left = x + "px";
        lens.style.top = y + "px";

        let fx = resultRect.width / lensRect.width;
        let fy = resultRect.height / lensRect.height;

        result.style.backgroundImage = `url('${image.src}')`;
        result.style.backgroundSize = `${image.width * fx}px ${image.height * fy}px`;
        result.style.backgroundPosition = `-${x * fx}px -${y * fy}px`;
    }

    function getMousePos(e) {
        const containerRect = container.getBoundingClientRect();
        return {
            x: e.clientX - containerRect.left - lens.offsetWidth / 2,
            y: e.clientY - containerRect.top - lens.offsetHeight / 2
        };
    }
});

function changeMainImage(productId, imageSrc) {
    const mainImage = document.querySelector(`#image-${productId}`);
    mainImage.src = `/productImages/${imageSrc}`;
}

function addToCheckout(productId) {
    // Make a POST request to the addToCheckout route with the product ID
    fetch(`/addToCheckout/${productId}`, {
        method: 'POST'
    })
    .then(response => {
        // Handle response, such as redirecting to the checkout page
        window.location.href = '/checkout';
    })
    .catch(error => {
        console.error('Error:', error);
    });
}
</script>

<%-include('footer')-%>
